package practice2;
/*Variables are static and final by default
*Can have static, abstract and default methods only
* public is the only access modifier that can be used in interface and given by default
* Abstract keyword is given by default to the abstract methods of interface
* An interface can not have instance var, method, constructors and blocks
*We can not create objects from interface
* */
public interface Flyable {
    public abstract String flyable();
}
